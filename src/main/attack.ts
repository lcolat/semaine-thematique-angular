class Attack {
    constructor(public readonly name: string, public readonly power: number, public readonly nature: string, public readonly type: string, public readonly accuracy: number) {
    }
}

export default Attack;